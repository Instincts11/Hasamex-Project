# Hasamex frontend

Next.js analyst dashboard. Run instructions and architecture live in the repo-root [README](../README.md).

```powershell
npx next dev -p 3000
```

The UI expects the API on `http://127.0.0.1:8000` and proxies `/backend/*` to it.
